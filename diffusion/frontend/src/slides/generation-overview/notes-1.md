Introduce the second key idea: **turning numbers into images with a diffusion model**.

- Once we have numbers that describe what we want, we need to actually create the image
- A diffusion model starts with pure noise (random pixel values) and gradually cleans it up
- The numbers from step 1 guide this process — they tell the model what the final image should look like
- The video shows this denoising process in action

Say: "Let's look at each of these steps in more detail."
