The second step is then **turning these numbers into images with a diffusion model**.

- Once we have numbers that describe what we want, we need to actually create the image

A diffusion model starts with pure noise random pixel values and gradually predicts an image

Let's look at the first step in more detail.
