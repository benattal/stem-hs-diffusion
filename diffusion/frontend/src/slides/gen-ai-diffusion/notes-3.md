Now show the video generation demo:

- Same idea, but the output is a full *video* instead of a single image
- Video diffusion is even more complex because the model has to generate consistent frames over time
- This technology is very new and rapidly improving
