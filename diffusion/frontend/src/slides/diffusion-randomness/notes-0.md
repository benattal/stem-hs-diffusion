An important thing to note here is that because the diffusion model starts from random noise, it produces random outputs.

If it starts with different random starting points, then it will produce different random images, even with the same text prompt!
