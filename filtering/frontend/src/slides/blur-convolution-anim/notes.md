Build through four steps. Step 1: "Here's our image, and here's the filter — a small grid of numbers called a kernel. This particular kernel has all ones, divided by nine. That's our box blur." Step 2: Zoom in to see the pixel values underneath. Step 3: "We place the kernel on top of a region of the image. We multiply each kernel value by the pixel underneath it, then sum all nine products. That gives us one output pixel." Step 4: "Then we slide the kernel one step and repeat — all the way across the entire image. Three operations: slide, multiply, sum. That's all filtering is. By changing the numbers in the kernel, we can do completely different things to the image."
