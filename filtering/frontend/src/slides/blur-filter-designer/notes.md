Interactive 5×5 filter designer. 

Demo: put 1s in the full middle row to show horizontal blur, then the full middle column for vertical blur — show the image update live. 

Then have students try something unexpected: a checkerboard pattern, or all weight in one corner (~60 seconds). 

Key takeaway: the kernel is just a recipe for mixing neighbouring pixels — the pattern of values determines what the filter does.
