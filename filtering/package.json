{
  "name": "filtering-workshop",
  "version": "1.0.0",
  "type": "module",
  "description": "Interactive presentation website for image filtering workshop",
  "private": true,
  "scripts": {
    "install:all": "cd frontend && npm install && cd ../backend && npm install",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "cd backend && node src/server.mjs"
  },
  "keywords": [
    "presentation",
    "filtering",
    "workshop",
    "react"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
