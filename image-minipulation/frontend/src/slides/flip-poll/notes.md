The answer is A: Reverse the order of columns.

For a horizontal flip (mirror effect), column 0 swaps with the last column, column 1 with second-to-last, and so on. This creates the mirror image effect. Reversing rows (option B) would be a vertical flip instead.
