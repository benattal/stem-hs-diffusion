lets visualize how light reflects off 2 points of our scene

When light hits an surface and reflects back, (for most surfaces), it scatters uniformly off the object

This means that all of the points in our scene emit light to every pixel on the sensor array

When the photons reach the sensor array, it has no way of knowing where the light came from
