The answer is A: Delete the unwanted rows and columns, keeping only the desired rectangle.

Cropping is simply selecting a sub-region of the pixel grid. You create a new, smaller grid that only contains the pixels within your desired rectangle. The rest are discarded.
