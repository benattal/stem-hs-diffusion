If we introduce a barrier with a very small opening, only one ray from each point on the scene can pass through.

Each ray maps to a unique point on the camera sensor array.

Now each exposed point on the scene maps to one point on the sensor array

By restricting the paths light can take, we formed an image.
